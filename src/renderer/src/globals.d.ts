/** 앱인토스 빌드 여부. vite.toss.config.ts 의 define 으로 치환된다. */
declare const __TOSS_BUILD__: boolean
/** 출시용 토스 빌드(npm run build:toss:release)에서만 true — 운영 광고 ID 를 쓸지 정한다 */
declare const __TOSS_ADS_LIVE__: boolean
